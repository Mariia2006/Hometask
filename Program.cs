using System;
namespace Program {
  class Program {
    static void Main (string[] args) {
      double a = ((0.3 * Math.Pow(5, 2) - 15) / (3.5 + Math.Pow(2, 2)));
      Console.WriteLine(a);
    }
  }
}
